<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>&lt;undefined&gt;</spriteName>
  <solid>0</solid>
  <visible>-1</visible>
  <depth>-5</depth>
  <persistent>-1</persistent>
  <parentName>&lt;undefined&gt;</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="0" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>textTimer = 120

firstKeyClick = false
keyTimer = textTimer

firstPageClick = false
pageTimer = textTimer

firstAllenClick = false
allenTimer = textTimer

firstScissorsClick = false
scissorsTimer = textTimer

firstSilverClick = false
silverTimer = textTimer

clockCompletionText = false
clockCompletionTimer = textTimer

combinationCompletionText = false
combinationCompletionTimer = textTimer

firstDoorOpenText = false
firstDoorOpenTimer = textTimer

keyCardText = false
keyCardTimer = textTimer

finalDoorNotCompleteText = false
finalDoorNotCompleteTimer = textTimer

swipeComplete = false
swipeTimer = textTimer

fileCabinetText = false
fileCabinetTimer = textTimer

lightOnText = false
lightOnTimer = textTimer

drawerOpenText = false
drawerOpenTimer = textTimer

cameraPickup = false
cameraTimer = textTimer

wholePageText = false
wholeTimer = textTimer

rightPageText = false
rightPageTimer = textTimer

pictureClick = false
pictureTimer = textTimer

windowClick = false
windowTimer = textTimer

stayClick = false
stayTimer = textTimer

letterClick = false
letterTimer = textTimer

dudClock = false
dudTimer = textTimer

markerClick = false
markerTimer = textTimer

codeFinished = false
codeTimer = textTimer

beforeFlash = false
beforeTimer = textTimer
stop = false
stopTimer = 45

doorUnlocked = false
doorTimer = textTimer

tissueClick = false
tissueTimer = textTimer
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="8" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>draw_set_font(doctorsFont)
draw_set_color($088a08)

//The text for the key pickup
if firstKeyClick == true and keyTimer &gt; 0{
    if keyTimer &gt; 0{
        //draw_set_font(doctorsFont)
        //draw_set_color($088a08)
        draw_text(275, 400, "Huh...a key?")
        keyTimer -= 1
    }
}
if room != pictureRoom and firstKeyClick == true{
keyTimer = 0
}

//The text for the page pickup
if firstPageClick == true and pageTimer &gt; 0{
    if pageTimer &gt; 0{
        draw_text(200, 400, "I wonder who ripped the page?")
        pageTimer -= 1
    }
}
if room != daddyRoom and firstPageClick == true{
pageTimer = 0
}

//The text for allen key pickup
if firstAllenClick == true and allenTimer &gt; 0{
    if allenTimer &gt; 0{
        draw_text(260, 380, "An allen key")
        allenTimer -= 1
    }
}
if room != daddyRoom and firstAllenClick == true{
allenTimer = 0
}

//The text for the scissors pickup
if firstScissorsClick == true and scissorsTimer &gt; 0{
    if scissorsTimer &gt; 0{
        draw_text(300, 380, "Hmmm can I cut something with this?")
        scissorsTimer -= 1
    }
}
if room != room2Table and firstScissorsClick == true{
scissorsTimer = 0
}

//The text for the silver key pickup
if firstSilverClick == true and silverTimer &gt; 0{
    if silverTimer &gt; 0{
        draw_text(280, 430, "Another key?")
        silverTimer -= 1
    }
}
if room != room2ClockRoom and firstSilverClick == true{
silverTimer = 0
}

//This displays text after clock completion
if everythingOverlord.clockComplete == true and room == windowRoom and clockCompletionTimer &gt; 0{
    if clockCompletionTimer &gt; 0{
        draw_text(245, 445, "It broke, I'll take the battery")
        clockCompletionTimer -= 1
    }
}
if room != windowRoom and clockCompletionText == true{
clockCompletionTimer = 0
}

//This displays text after combination completion
if everythingOverlord.combinationComplete == true and room == room2DontLeave and combinationCompletionTimer &gt; 0{
    if combinationCompletionTimer &gt; 0{
        draw_text(490, 445, "It was Correct!")
        combinationCompletionTimer -= 1
    }
}
if room != room2DontLeave and combinationCompletionText == true{
combinationCompletionTimer = 0
}

//This displays text after the first door is opened with the golden key

if firstDoorOpenText == true and firstDoorOpenTimer &gt; 0 and room == room2Table{
    if firstDoorOpenTimer &gt; 0{
        draw_text(260, 285, "Yes, the door unlocked")
        firstDoorOpenTimer -= 1
    }
}
if room != room2Table and firstDoorOpenText == true{
firstDoorOpenTimer = 0
}

//This displays the text for the keycard text
if keyCardText == true and keyCardTimer &gt; 0 and room == pictureRoom{
    if keyCardTimer &gt; 0{
        draw_text(150, 450, "Wait, a keycard was in the pillow?")
        keyCardTimer -= 1
    }
}
if room != pictureRoom and keyCardText == true{
keyCardTimer = 0
}

//Final Door not completed Text

if finalDoorNotCompleteText == true and finalDoorNotCompleteTimer &gt; 0 and room == room2DontLeave{
    if finalDoorNotCompleteTimer &gt; 0{
        
        if combinationCompletionText == true and keyCardText = false{
        draw_text(70, 228, "Looks like I need to swipe a card...")
        }
        else if combinationCompletionText == false and keyCardText = true{
        draw_text(70, 228, "The combination needs to be put in")
        }
        else if combinationCompletionText == false and keyCardText = false{
        draw_text(70, 228, "How do I open this door?")
        }
        finalDoorNotCompleteTimer -= 1
    }
}

if finalDoorNotCompleteTimer &lt;= 0{
finalDoorNotCompleteText = false
finalDoorNotCompleteTimer = textTimer
}
if room != room2DontLeave and finalDoorNotCompleteText == true{
finalDoorNotCompleteTimer = 0
}

//KeyCard Swipe Text
if swipeComplete == true and swipeTimer &gt; 0 and room == room2DontLeave{
    if swipeTimer &gt; 0{
        draw_text(70, 228, "I swiped the keycard")
        swipeTimer -= 1
    }
}
if room != room2DontLeave and swipeComplete == true{
swipeTimer = 0
}

//Text for opening the file cabinet
if fileCabinetText == true and fileCabinetTimer &gt; 0 and room == closetWall{
    if fileCabinetTimer &gt; 0{
        draw_text(150, 250, "The key fit perfectly")
        fileCabinetTimer -= 1
    }
}
if room != closetWall and fileCabinetText == true{
fileCabinetTimer = 0
}

//Text for turning on the light
if lightOnText == true and lightOnTimer &gt; 0 and room == closetWall{
    if lightOnTimer &gt; 0{
        draw_text(300,280, "Oh, a closet")
        lightOnTimer -= 1
    }
}
if room != closetWall and lightOnText == true{
lightOnTimer = 0
}

//Text for drawerOpen
if drawerOpenText == true and drawerOpenTimer &gt; 0 and room == room2ClockRoom{
    if drawerOpenTimer &gt; 0{
        draw_text(280,450, "What just popped out?")
        drawerOpenTimer -= 1
    }
}
if room != room2ClockRoom and drawerOpenText == true{
drawerOpenTimer = 0
}

//Text for cameraPickup
if cameraPickup == true and cameraTimer &gt; 0 and room == room2Table{
    if cameraTimer &gt; 0{
        draw_text(250,320, "The camera doesn't turn on...")
        cameraTimer -= 1
    }
}
if room != room2Table and cameraPickup == true{
cameraTimer = 0
}
//Text for wholePageText

if wholePageText == true and wholeTimer &gt; 0 and room == room2Table{
    if  wholeTimer &gt; 0{
        draw_text(280,150, "The two pages fit together nicely")
        wholeTimer -= 1
    }
}
if room != room2Table and wholePageText == true{
wholeTimer = 0
}
//Text for rightPageText
if rightPageText == true and rightPageTimer &gt; 0 and room == room2Table{
    if  rightPageTimer &gt; 0{
        draw_text(278,350, "The numbers on this page must mean something")
        rightPageTimer -= 1
    }
}
if room != room2Table and rightPageText == true{
rightPageTimer = 0
}

//Text when randomObject picture is clicked
if pictureClick == true and pictureTimer  &gt; 0 and room == pictureRoom{
    if  pictureTimer &gt; 0{
        draw_text(240,160, "What....odd....drawings")
        pictureTimer -= 1
    }
}
if room != pictureRoom and pictureClick == true{
pictureTimer = 0
}

//Text when window is clicked
if windowClick == true and windowTimer  &gt; 0 and room == windowRoom{
    if  windowTimer &gt; 0{
        draw_text(490,256, "Lovely view")
        windowTimer -= 1
    }
}
if room != windowRoom and windowClick == true{
windowTimer = 0
}

//Text when stayClick is clicked
if stayClick == true and stayTimer  &gt; 0 and room == stayRoom{
    if  stayTimer &gt; 0{
        draw_text(280,130, "S.T.A.Y??")
        stayTimer -= 1
    }
}
if room != stayRoom and stayClick == true{
stayTimer = 0
}

//Text whe letterClick is clicked
if letterClick == true and letterTimer  &gt; 0 and room == room2ComeBack{
    if  letterTimer &gt; 0{
        draw_text(416,352, "Come back where?")
        letterTimer -= 1
    }
}
if room != room2ComeBack and letterClick == true{
letterTimer = 0
}

//Text when dudClock is clicked
if dudClock == true and dudTimer  &gt; 0 and room == room2ClockRoom and everythingOverlord.clockComplete == false{
    if  dudTimer &gt; 0{
        draw_text(384,160, "..Hmmm...familiar")
        dudTimer -= 1
    }
}
if room != room2ClockRoom and dudClock == true{
dudTimer = 0
}

//Text when markers are clicked
if markerClick == true and markerTimer  &gt; 0 and room == closetDoor{
    if  markerTimer &gt; 0{
        draw_text(140,416, "Markers...useless")
        markerTimer -= 1
    }
}
if room != closetDoor and markerClick == true{
markerTimer = 0
}

//Text when briefcase code is finished
if codeFinished == true and codeTimer  &gt; 0 and room == closetWall{
    if  codeTimer &gt; 0{
        draw_text(250,340, "The briefcase opened!")
        codeTimer -= 1
    }
}
if room != closetWall and codeFinished == true{
codeTimer = 0
}
//Text when cutscene starts
if beforeFlash == true and stop == false and room != briefcaseRoom and room != keypadRoom and room != insideBriefcase and room != clockRoom{
draw_text(256,192, "Dr. Newman is...")
}

//Text when cutscene ends
if stop == true and stopTimer &gt; 0{
draw_text(256,192, "......me")
}
if stopTimer &gt; 0 and stop == true{
stopTimer -= 1
}

//Text when door hasn't been opened
if doorUnlocked == true and doorTimer &gt; 0 and room == stayRoom{
    if doorTimer &gt; 0{
        draw_text(430, 400, "The door is locked")
        doorTimer -= 1
    }
}
if room != stayRoom and doorUnlocked == true{
doorTimer = 0
}

//Text for tissue
if tissueClick == true and tissueTimer &gt; 0 and room == closetDoor{
    if tissueTimer &gt; 0{
        draw_text(416, 416, "Tissue....useless")
        tissueTimer -= 1
    }
}
if room != closetDoor and tissueClick == true{
tissueTimer = 0
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>0</PhysicsObject>
  <PhysicsObjectSensor>0</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0.100000001490116</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0.100000001490116</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0.200000002980232</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints/>
</object>
